{
  "name": "naughty-pi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "connect": "ssh pi@raspberrypi.local",
    "docker:build": "docker build -t streaming .",
    "docker:run": "docker run --privileged --name meow -d --restart=always -e STREAM_KEY=$STREAM_KEY streaming:latest",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "sudo node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "googleapis": "^84.0.0"
  }
}
